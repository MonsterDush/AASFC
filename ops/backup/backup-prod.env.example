# Copy to /etc/axelio/backup-prod.env with mode 0600.
# The backend DATABASE_URL and RELEASE_VERSION are loaded from backend/.env.
BACKUP_ENCRYPTION_PASSWORD=replace-with-a-long-random-secret
BACKUP_RCLONE_REMOTE=s3-compatible:axelio-production-backups
RCLONE_CONFIG=/etc/axelio/rclone.conf
